// import Evenement from '../models/Evenement'

const state = {
  fetched: false
}

const mutations = {
  SET_FETCHED (state, fetched) {
    state.fetched = fetched
  }
}

const actions = {
}

export default {
  state,
  mutations,
  actions
}
